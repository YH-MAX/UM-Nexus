/** Set when Google OAuth returns a non-UM email; AuthForm shows the domain error. */
export const AUTH_DOMAIN_REJECTED_KEY = "um_nexus_auth_domain_error";

/** Set when Google OAuth fails or session is invalid; AuthForm shows the message. */
export const AUTH_OAUTH_ERROR_KEY = "um_nexus_oauth_error";
